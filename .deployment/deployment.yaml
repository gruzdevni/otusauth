apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-dp
  labels:
    app: auth-dp
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      env: v1
  template:
    metadata:
      labels:
        env: v1
    spec:
      containers:
      - name: auth-app
        image: nikolaygr/otusauth:v0.7
        envFrom:
         - configMapRef:
            name: conf-map
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: auth-psql-1-postgresql               
              key: postgres-password
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /api/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 3