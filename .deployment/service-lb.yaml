apiVersion: v1
kind: Service
metadata:
  name: auth-service-lb
  labels:
    app: auth-service-lb
spec:
  selector:
    env: v1
  ports:
    - protocol: TCP
      name: web
      port: 80
      targetPort: 8080
  type: LoadBalancer