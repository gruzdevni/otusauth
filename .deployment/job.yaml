apiVersion: batch/v1
kind: Job
metadata:
  name:  migrate-postgres
spec:
  template:
    spec:
      containers:
      - name: migrate-postgres
        image: nikolaygr/otusauth:v0.6
        command: ["/otusauth", "postgres", "up"]
        envFrom:
         - configMapRef:
            name: conf-map
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: auth-psql-1-postgresql
              key: postgres-password
      restartPolicy: Never   
  backoffLimit: 4